box: wercker-labs/docker
build:
  steps:
    - script:
        name: Build a CentOS 6.6 Docker Hatohol image box
        code: |
          docker -v
          docker build -t centos66/build-hatohol wercker/
          mv /root/rpmbuild/RPMS/x86_64/*.rpm $WERCKER_OUTPUT_DIR